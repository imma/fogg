---
- debug: var=env
- debug: var=color
- debug: var=app
- debug: var=service
- local_action:
    module: ec2_elb_facts
    names:
    - "{{ env }}-{{ app }}-{{ service }}-{{ color }}"
  register: elb

- local_action:
    module: debug
    msg: "{{ item }}"
  with_items: "{{ elb.elbs }}"
