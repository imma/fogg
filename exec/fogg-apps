#!/usr/bin/env bash

function main {
  local shome="${_fogg_home:-"$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"}"
  source "$shome/script/profile"

  ln -nfs ../env-xxx/env.tf .
}

source sub "$BASH_SOURCE" "$@"
