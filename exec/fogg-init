#!/usr/bin/env bash

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"
  source normalize

  fogg default
  tf init -backend-config .backend-config.tfvars "$@"
}

source sub "$BASH_SOURCE" "$@"
